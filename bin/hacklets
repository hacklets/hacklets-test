#!/bin/bash

source hacklets.source

hacklets_debug_state

#TODO: implement this

# here be basically do VAR1=VAL1... /bin/bash COMMAND
# where VAR are HACKLETS_* variables
# this is basically a porcelain interface for the
# plumbing interface via functions from hacklets.sh

# commands will be added gradually, as they become stable
# however, this is a hacking project, so the plumbing interface
# should not really be hidden
