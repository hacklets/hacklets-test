#!/bin/bash

source ${0%/*}/scripts.d/core.sh

if [ ! -n "$1" ]; then
    echo "Oops! Pass a name.";
    exit 1;
fi

if [ ! -n "$2" ]; then
    echo "Oops! Pass a repository.";
    exit 1;
fi

set -e

mkdir -p $GIT_HACKLET_BASEDIR_NAME

hacklet_fetch "$1" "$2"
hacklet_install "$1"
